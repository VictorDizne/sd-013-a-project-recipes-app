import React from 'react';

const HomeFood = () => (
  <div>
    Home Food
  </div>
);

Login.propTypes = {
  history: PropTypes.shape({
    push: PropTypes.func,
  }).isRequired,
};

export default HomeFood;
