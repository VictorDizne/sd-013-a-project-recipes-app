import React from 'react';
import App from '../App';
import Login from '../pages/Login';
import { render } from '@testing-library/react';
import renderWithRouter from '.RenderWithRouter';


describe('Testando tela Login')
// se a rota ta certa
// testa titulo, inputs e botoes
// se botao habilita com email senha correto
//componentes na tela
// botao comeca disabled
// fireevent.type / click - se o input escreveuo que digitei (olhar no state)
